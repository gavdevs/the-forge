import { router, publicProcedure } from '../../../trpc';
import { z } from 'zod';

export const aiRouter = router({
  generate: publicProcedure
    .input(z.object({ prompt: z.string() }))
    .mutation(async ({ input }): Promise<{ text: string }> => {
      // TODO: implement with AI SDK
      return { text: '' };
    }),
});
